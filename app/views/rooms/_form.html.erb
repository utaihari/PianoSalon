<%= form_for(room) do |f| %>
<% if room.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

  <ul>
    <% room.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<%= f.hidden_field :area_id, :value => @area.id %>

<%= f.label :部屋の名前 %>
<%= f.text_field :room_name %>

<br>
<div class="field">
  <%= f.label :部屋の紹介 %>
  <%= f.text_area :description %>
</div>

<div class="actions">
  <%= f.submit %>
</div>
<% end %>

<script>
 $(function() {
  $('#room_description').froalaEditor({
  });
});
</script>
